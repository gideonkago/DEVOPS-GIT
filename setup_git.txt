
cd d:
cd Academy:
cd GIT:

git config --global user.name "gideonkago"
git config --global user.email gideonkago@gmail.com

git config --list #list configs
git add .# add file to git repository
git status # New file was added
git add <file> # update a file
git help config # get a help on 
$ git commit -m 'initial_project_version' # commit
$ git log
$git diff XXXXXXX    YYYYYYYY
$ git remote add origin https://github.com/gideonkago/DEVOPS-GIT.git

$ git pull --rebase origin master
$ git push -u origin master
$ git branch new_branch
$ git checkout new_branch
$ git push -u origin new_branch
$ git clone https://github.com/myliang/x-spreadsheet.git
git log index.html  # query changes on file index.html  # Date:   Fri Jan 25 10:26:17 2019 +0300



$ git push all

